[build]
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -c 'cd packages/db && pnpm exec prisma db push --accept-data-loss && cd /app && node apps/api/dist/main'"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
