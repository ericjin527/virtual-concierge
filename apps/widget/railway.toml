[build]
dockerfilePath = "apps/widget/Dockerfile"

[deploy]
startCommand = "sh -c 'cd apps/widget && pnpm exec next start'"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
